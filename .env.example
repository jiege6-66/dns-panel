# 环境配置
NODE_ENV=development

# 服务器配置
PORT=4001

# 数据库配置
DATABASE_URL="file:./database.db"

# JWT 配置
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# 加密密钥（用于加密 Cloudflare API Token，必须是 32 字符）
ENCRYPTION_KEY="change-this-to-32-character-key!"

# ACME / 证书配置
ACME_ENV=staging
CERTIFICATE_SCHEDULER_INTERVAL_MS=15000
CERTIFICATE_RENEWAL_SCHEDULER_INTERVAL_MS=21600000
ACME_PROPAGATION_DELAY_MS=30000
VENDOR_CERTIFICATE_SCHEDULER_INTERVAL_MS=30000
CERTIFICATE_DEPLOY_SCHEDULER_INTERVAL_MS=30000

# Cloudflare 优选部署工作流
OPTIMIZED_DEPLOYMENT_SCHEDULER_INTERVAL_MS=15000
OPTIMIZED_POLL_INTERVAL_MS=10000
OPTIMIZED_FALLBACK_TIMEOUT_MS=300000
OPTIMIZED_SSL_TIMEOUT_MS=600000
OPTIMIZED_HOSTNAME_TIMEOUT_MS=600000
OPTIMIZED_HEALTH_TIMEOUT_MS=15000

# SMTP 配置（用于邮件通知，可选；未在设置中配置 SMTP 时使用）
# SMTP_HOST="smtp.example.com"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="user@example.com"
# SMTP_PASS="your-smtp-password"
# SMTP_FROM="DNS Panel <no-reply@example.com>"

# CORS 配置
CORS_ORIGIN="http://localhost:5173"

# 日志配置
LOG_RETENTION_DAYS=90
